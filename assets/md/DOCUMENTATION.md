## 📘 Документация проекта (подробная версия)

Ниже представлен **подробный** вариант `DOCUMENTATION.md`. Он описывает каждый компонент, его назначение, а также содержит инструкции для разработки.

---

```markdown
# Документация сайта hrlubacheva.github.io (подробная)

## 1. Общее описание

Проект представляет собой одностраничный сайт-визитку для карьерного консультанта, HR-эксперта и бизнес-тренера Виктории Любачевой.

**Основные функции:**
- Калькулятор услуг с корзиной, скидкой 5% и кастомными выпадающими списками.
- Квиз (5 вопросов) с экраном подтверждения ответов и подбором варианта решения.
- Форма бесплатной 20-минутной диагностики (обратный звонок).
- Раздача бесплатных материалов (PDF: чек-лист и программа тренинга).
- Адаптивный дизайн, плавные анимации при скролле, работа офлайн (через Service Worker).

**Технологии:** HTML5, CSS3, JavaScript (ES6), Python (сборка), Google Apps Script (обработка форм), GitHub Pages (хостинг).

---

## 2. Полная структура файлов и папок

```
hrLubacheva.github.io/
│
├── components/                       # Исходные файлы (только здесь вносить правки)
│   ├── common/                       # Общие для всего сайта блоки
│   │   ├── 00_head.html              # Заголовок, мета-теги, шрифты, счётчики
│   │   ├── 01_navbar.html            # Верхняя навигация (лого, бургер, соцсети)
│   │   ├── 02_footer.html            # Подвал с копирайтом и ссылкой на политику
│   │   ├── 03_cookie-banner.html     # Баннер согласия на cookie
│   │   ├── 04_privacy-modal.html     # Модальное окно политики конфиденциальности
│   │   ├── 05_checklist-modal.html   # Модалка для отправки email и получения PDF
│   │   ├── css/
│   │   │   └── 00_base.css           # ЕДИНЫЙ файл всех стилей (31 КБ)
│   │   └── js/
│   │       ├── 00_config.js          # Глобальные константы (URL, ID счётчиков, контакты)
│   │       ├── 00_core.js            # Утилиты: логгеры, тосты, гео, UTM, localStorage
│   │       ├── 01_animations.js      # Базовый Intersection Observer для fade-up
│   │       ├── 02_forms.js           # Валидация и отправка формы обратного звонка
│   │       ├── 03_modal.js           # Открытие/закрытие модальных окон
│   │       ├── 04_cookie-consent.js  # Запись согласия на cookie и управление аналитикой
│   │       ├── 05_navigation.js      # Плавный скролл по якорям и бургер-меню
│   │       ├── 06_copy.js            # Копирование в буфер телефона и @HrLubacheva
│   │       ├── 07.services-data.js # Данные услуг (цены, названия) для калькулятора
│   │       ├── 07_calculator.js      # Логика калькулятора: корзина, кастомные дропдауны
│   │       ├── 08.quiz-matrix.js   # Матрица вариантов для квиза (около 200 записей)
│   │       ├── 08_quiz.js            # Логика квиза: 5 вопросов + подтверждение + результат
│   │       ├── 10_email.js           # Отправка запроса на получение материалов (email)
│   │       └── 99_public-main.js     # Точка входа: инициализация всех модулей, переопределение анимации для динамики
│   │
│   └── sections/                     # HTML-секции (порядок сборки = алфавитный порядок имён)
│       ├── 01_hero.html              # Блок с фото, заголовком, кнопкой Telegram
│       ├── 02_roles.html             # Мои экспертные роли (5 карточек)
│       ├── 03_services.html          # Услуги (4 карточки + ссылка на PDF)
│       ├── 04_stats.html             # Цифры с достижениями (6 блоков)
│       ├── 05_benefits.html          # Преимущества (4 карточки на тёмном фоне)
│       ├── 06_process.html           # Как работаем: этапы для консультации и подбора
│       ├── 07_calculator.html        # Калькулятор услуг (4 вкладки, корзина)
│       ├── 08_quiz.html              # Контейнер для квиза (пустой, заполняется JS)
│       ├── 09_freebies.html          # Бесплатные материалы (3 кнопки)
│       ├── 10_calendar.html          # Форма бесплатной диагностики
│       └── 11_contacts.html          # Контакты: email, мессенджеры, телефон
│
├── assets/                           # Статические файлы
│   ├── docs/                         # PDF-файлы
│   │   ├── checklist.pdf             # Чек-лист «Идеальное резюме»
│   │   └── training_program.pdf      # Программа тренинга «Продай себя дорого»
│   └── images/
│       └── img_1.jpg                 # Фотография Виктории (используется в hero, OG, Twitter)
│
├── build.py                          # Скрипт сборки (Python 3)
├── server.py                         # Локальный сервер для разработки (с отключением кеша)
├── sw.js                             # Пустой Service Worker (заглушка, чтобы не было 404)
├── robots.txt                        # Разрешает индексацию всех страниц
├── sitemap.xml                       # Карта сайта для поисковых систем
├── index.html                        # ГЕНЕРИРУЕТСЯ, НЕ РЕДАКТИРОВАТЬ ВРУЧНУЮ
└── DOCUMENTATION.md                  # Этот файл
```

---

## 3. Описание ключевых JavaScript-модулей

### 00_core.js
- Содержит вспомогательные функции: `escapeHtml`, `log*`, `showToast`, `getOrCreateLocalUserId`, `sendDataToSheet`, `formatPhoneNumber`, `getUTMParams`, `getGeoData` и т.д.
- Определяет `IS_DEV` (отключает логи в продакшене).
- Сохраняет `user_id` в `localStorage` для аналитики.
- Отвечает за сбор геоданных через `ipapi.co`.

### 07_calculator.js
- Управляет корзиной (`cart`).
- Функция `initCustomDropdowns` заменяет стандартные `<select>` на кастомные кнопки с выпадающим меню.
- Функция `renderCart` отображает добавленные услуги, считает итог и скидку.
- Данные услуг берутся из `window.LOCAL_SERVICES` (файл `07.services-data.js`).

### 08_quiz.js
- Реализует квиз с 5 вопросами.
- В зависимости от выбранного уровня, второй вопрос меняется (для рекрутеров – другой текст).
- После ответа на 5-й вопрос автоматически открывается экран подтверждения (все ответы сводкой).
- Кнопка «Показать варианты» запускает поиск в матрице `window.VARIANTS_MATRIX` и выводит 3 варианта.
- Выбранный вариант отправляется в Google Sheets.

### 99_public-main.js
- Главный скрипт, запускается после загрузки DOM.
- Инициализирует все модули: анимацию, формы, модалки, калькулятор, квиз, навигацию, cookie, кнопку «Наверх».
- Переопределяет `initCalculator` и `initQuiz`, чтобы после их выполнения перезапускать анимацию для новых элементов.
- Вешает обработчик на клик по вкладкам калькулятора – анимация обновляется.
- При `load` принудительно показывает элементы с классом `.fade-up`, которые уже видны.

---

## 4. Как работать с проектом (пошагово)

### 4.1. Локальная разработка

```bash
# 1. Собрать index.html из компонентов
python build.py

# 2. Запустить локальный сервер (порт 8080)
python server.py
```

Сайт станет доступен по адресу `http://127.0.0.1:8080`.  
При любом изменении в `components/` нужно **заново выполнить `build.py`**, затем обновить страницу в браузере (лучше `Ctrl+Shift+R`, чтобы сбросить кеш).

### 4.2. Редактирование контента

- **Текст, HTML-структура** – править файлы в `components/sections/`.
- **Глобальные стили** – только `components/common/css/00_base.css`.
- **JavaScript-логика** – править соответствующие файлы в `components/common/js/`.

### 4.3. Добавление новой секции

1. Создать новый HTML-файл в `components/sections/` с именем, например, `12_clients.html`.
2. Вставить код секции. Рекомендуется добавить контейнеру класс `fade-up`, если нужна анимация при скролле.
3. Запустить `build.py`. Секция появится в конце страницы (порядок сортируется по имени файла).

### 4.4. Обновление PDF-файлов

1. Заменить `checklist.pdf` и/или `training_program.pdf` в папке `assets/docs/`.
2. Сохранить те же имена.
3. Выполнить `git add`, `commit`, `push`.

Ссылки на сайте остаются теми же, файлы обновятся автоматически.

### 4.5. Публикация на GitHub Pages

```bash
python build.py               # собрать свежий index.html
git add .
git commit -m "описание изменений"
git push origin main
```

Через 1–2 минуты сайт обновится по адресу `https://hrlubacheva.github.io/`.

---

## 5. Важные технические нюансы

### 5.1. Анимация `fade-up`
- Элемент с классом `.fade-up` изначально прозрачен и сдвинут вниз.
- При попадании в область видимости (через Intersection Observer) добавляется класс `.visible`, который делает его видимым.
- В `99_public-main.js` есть функция `reinitAnimations`, которая следит за новыми элементами, добавляемыми после подгрузки калькулятора или квиза.

### 5.2. Кастомные выпадающие списки
- Обычные `<select class="service-select">` скрываются (`display: none`).
- Взамен создаётся `<div class="custom-dropdown">` с кнопкой и меню.
- Стили описаны в `00_base.css` (блок в конце файла). Поддерживают плавную анимацию, тени, акцент при наведении, скроллбар.

### 5.3. Cookie-баннер и аналитика
- Яндекс.Метрика и Google Analytics загружаются сразу, но по умолчанию имеют `analytics_storage: 'denied'`.
- После нажатия «Принять» в `localStorage` сохраняется `cookie_consent = 'accepted'`, и счётчики обновляют настройки через `gtag('consent', 'update', ...)`.
- Если пользователь отклонил – баннер больше не показывается, аналитика не активируется.

### 5.4. Формы и отправка данных
- Все формы (обратный звонок, запрос материалов, выбор в квизе) собирают данные и отправляют POST-запросом на `SCRIPT_URL` (Google Apps Script).
- Режим `mode: 'no-cors'` – ответ не обрабатывается, но данные гарантированно попадают в таблицу.
- В данные автоматически добавляются: `userId`, `utm`, `device`, `page`, `geo`, `timeOnSite`, `visitStats`.
- В файле `00_config.js` указан `ADMIN_EMAIL`, `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID` – они используются в скрипте Google Apps Script для уведомлений (сами скрипты не входят в репозиторий, они лежат на стороне Google).

### 5.5. Service Worker (`sw.js`)
- Файл создан, чтобы браузер не выдавал ошибку 404 при попытке зарегистрировать Service Worker.
- Он абсолютно пустой (только заглушка), не кеширует ничего. Сайт и так работает офлайн благодаря стандартному кешированию браузера.

---

## 6. Полезные команды (шпаргалка)

| Команда | Действие |
|---------|----------|
| `python build.py` | Пересобрать `index.html` |
| `python server.py` | Запустить локальный сервер (http://127.0.0.1:8080) |
| `git add . && git commit -m "message" && git push` | Обновить сайт на GitHub Pages |
| `rm -f index.html && python build.py` | Принудительно пересобрать (если index.html повреждён) |

---

## 7. Контакты и ссылки

- **Репозиторий**: `https://github.com/hrLubacheva/hrLubacheva.github.io`
- **Живой сайт**: `https://hrlubacheva.github.io`
- **Google Sheets (данные форм)**: скопировать из `00_config.js` → `SCRIPT_URL` (это веб-приложение, таблица связана с ним)
- **Автор / поддержка**:
  - Telegram: `@HrLubacheva`
  - WhatsApp: `+7 921 791-66-55`
  - Email: `hrlubacheva@mail.ru`

---

*Документация актуальна на май 2026 года. При изменении архитектуры проекта обновите этот файл.*
```